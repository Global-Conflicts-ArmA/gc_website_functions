name = "GC Website Functions"
prefix = "gc_WebsiteFunctions"
author = "W-Cephei"
files = [
    "mod.cpp"
]

key_name = "{{prefix}}_{{version}}"

releasebuild = [
    "@zip gc_WebsiteFunctions_{{semver.major}}.{{semver.minor}}.{{semver.patch}}.{{semver.build}}"
]

